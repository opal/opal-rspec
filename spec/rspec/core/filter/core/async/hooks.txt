# promise, see opal alternates
RSpec::Core::Hooks when an error happens in .* allows the error to propagate to the user
RSpec::Core::Hooks#around considers the hook to have run when passed as a block to a method that yields
