# This stub file is needed because opal cannot parse this ruby code:
#
# def raise_block_syntax_error
#   raise SyntaxError.new(<<-MESSAGE)
#     block passed to should or should_not change must use {} instead of do/end
#   MESSAGE
# end
